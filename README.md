# PyDopplerSim
A basband signal (RF) simulator that models Doppler behavior of a concurrently mobile RX (receiver) and TX (transmitter).

Why is PyDopplerSim unique? It combines...
- Mobile RX _and_ TX
- True physical layer baseband signal processing through IQ values
- AWGN modelling
- Python framework for easy porting to GNU Radio blocks
